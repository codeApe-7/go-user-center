# 复用现有 MySQL（mysql-production）
# 先在 mysql-production 中创建库和账号：
# CREATE DATABASE IF NOT EXISTS go_user_center CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
# CREATE USER IF NOT EXISTS 'go_uc'@'%' IDENTIFIED BY 'change_me';
# GRANT ALL PRIVILEGES ON go_user_center.* TO 'go_uc'@'%';
# FLUSH PRIVILEGES;

DB_DSN=go_uc:change_me@tcp(mysql-production:3306)/go_user_center?charset=utf8mb4&parseTime=True&loc=Local
JWT_SECRET=change_me_to_a_long_random_secret
